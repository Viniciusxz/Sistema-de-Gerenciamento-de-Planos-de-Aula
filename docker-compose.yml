version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - API_PORT=3000
    volumes:
      - ./src/database:/app/src/database